swagger: '2.0'
info:
  version: v1
  title: employer
  description: This is sample API being generated by cedrus-api generator
  x-ibm-name: metlifeapis
host: $(catalog.host)
basePath: /metlife
schemes:
  - https
produces:
  - application/json
paths:
  /v1/Employers:
    get:
      tags:
        - Employer
      description: Gets all Employers from the system that the user has access to
      operationId: getEmployer
      produces:
        - application/json
      x-swagger-router-controller: Employer
      responses:
        '200':
          description: Employer response
          schema:
            type: array
            items:
              $ref: '#/definitions/v1Employers'
        '404':
          description: Employer response
          schema:
            type: object
        '500':
          description: Employer response
          schema:
            type: object
      parameters:
        - name: pageSize
          in: query
          description: Number of records returned
          type: integer
        - name: pageNumber
          in: query
          description: Page number
          type: integer
    put:
      tags:
        - Employer
      description: Puts all Employers from the system that the user has access to
      operationId: putEmployer
      produces:
        - application/json
      consumes:
        - application/json
      x-swagger-router-controller: Employer
      responses:
        '204':
          description: Employer response
          schema:
            type: array
            items:
              $ref: '#/definitions/v1Employers'
        '404':
          description: Employer response
          schema:
            type: object
        '500':
          description: Employer response
          schema:
            type: object
      parameters:
        - name: v1/Employers
          in: body
          schema:
            $ref: '#/definitions/v1Employers'
    post:
      tags:
        - Employer
      description: Posts all Employers from the system that the user has access to
      operationId: postEmployer
      produces:
        - application/json
      consumes:
        - application/json
      x-swagger-router-controller: Employer
      responses:
        '204':
          description: Employer response
          schema:
            type: array
            items:
              $ref: '#/definitions/v1Employers'
        '404':
          description: Employer response
          schema:
            type: object
        '500':
          description: Employer response
          schema:
            type: object
      parameters:
        - name: v1/Employers
          in: body
          schema:
            $ref: '#/definitions/v1Employers'
    patch:
      tags:
        - Employer
      description: Patchs all Employers from the system that the user has access to
      operationId: patchEmployer
      produces:
        - application/json
      consumes:
        - application/json
      x-swagger-router-controller: Employer
      responses:
        '204':
          description: Employer response
          schema:
            type: array
            items:
              $ref: '#/definitions/v1Employers'
        '404':
          description: Employer response
          schema:
            type: object
        '500':
          description: Employer response
          schema:
            type: object
definitions:
  ApiError:
    type: object
    properties:
      code:
        type: integer
        format: int32
      message:
        type: string
      fields:
        type: string
  v1Employers:
    type: object
    properties:
      Employer_name:
        type: string
      Address:
        type: object
        properties:
          Street Name:
            type: string
          City:
            type: string
          State:
            type: string
          P.o.Box:
            type: number
          Country:
            type: string
        required:
          - Street Name
          - City
          - State
          - P.o.Box
          - Country
      Phone_number:
        type: object
        properties:
          home phone:
            type: string
          cell phone:
            type: string
        required:
          - home phone
          - cell phone
      size:
        type: string
    required:
      - Employer_name
      - size
responses:
  Standard500ErrorResponse:
    description: An unexpected error occured.
    schema:
      $ref: '#/definitions/ApiError'
  PersonDoesNotExistResponse:
    description: Person does not exist.
x-ibm-configuration:
  testable: true
  enforced: true
  cors:
    enabled: true
  gateway: datapower-gateway
  catalogs:
    apic-dev:
      properties:
        runtime-url: $(TARGET_URL)
    sb:
      properties:
        runtime-url: 'http://localhost:8080'
  assembly:
    execute:
      - invoke:
          target-url: 'https://metlifeapis.mybluemix.net$(request.path)$(request.search)'
          tls-profile: ''
          secure-gateway: false
    catch: []
